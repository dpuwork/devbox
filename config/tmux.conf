# --- Custom Devbox TMUX Additions (Local Patch) ---
# Enable mouse support (handles scrolling, pane selection, and prevents raw mouse escape codes in Ghostty)
set -g mouse on

# Copy current pane_id to clipboard using pbcopy on prefix + I
bind I run-shell "tmux display-message -p '#{pane_id}' | pbcopy"
